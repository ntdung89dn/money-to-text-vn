export declare function convertDozens(so: number, daydu: boolean): string;
export declare function readBlock(so: number, daydu: boolean): string;
export declare function convertMilions(so: number, daydu: boolean): string;
export declare function converMoneyToText(so: number): string;
